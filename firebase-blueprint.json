{
  "entities": {
    "saved_predictions": {
      "title": "Saved Prediction",
      "description": "User saved exclusion predictions",
      "type": "object",
      "properties": {
        "period": { "type": "string" },
        "predictedNumbers": { "type": "array", "items": { "type": "integer" } },
        "createdAt": { "type": "string" },
        "userId": { "type": "string" }
      }
    }
  }
}
